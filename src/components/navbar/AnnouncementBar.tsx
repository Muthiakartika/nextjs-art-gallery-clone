import { ANNOUNCEMENT } from "@/components/navbar/navItems";

export default function AnnouncementBar() {
  return (
    <div className="bg-[#26201b] px-4 py-2 text-center text-[11px] font-medium uppercase tracking-[0.2em] text-[#f4efe9]">
      {ANNOUNCEMENT}
    </div>
  );
}
