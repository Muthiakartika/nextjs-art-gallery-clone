import { ANNOUNCEMENT } from "@/components/navbar/navItems";

export default function AnnouncementBar() {
  return (
    <div className="bg-text px-4 py-2 text-center text-[11px] font-medium uppercase tracking-[0.2em] text-background">
      {ANNOUNCEMENT}
    </div>
  );
}
